extends ../layout

block content
  section.section.section-artwork
    .artwork-layout
      .artwork-image
        if artwork.image_url
          img(src=artwork.image_url, alt=artwork.title)
        else
          .art-placeholder-large #{artwork.title.charAt(0)}

      .artwork-details
        h2.section-title= artwork.title
        if artwork.artist_name
          p.section-subtitle by #{artwork.artist_name}
        if artwork.description
          p.art-description= artwork.description

        .artwork-actions
          // ✅ NEW: Edit the saved artwork (loads state_json into editable canvas)
          a.btn.btn-primary(href=`/artwork/${artwork.id}/edit`) Edit Artwork

          a.btn.btn-outline(href="/gallery") ← Back to gallery
